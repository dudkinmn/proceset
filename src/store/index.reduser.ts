export type TAction = {
  type: string;
  payload?: any;
};

export const getReduser = (state: any, action: TAction): any => {
  switch (action.type) {
    case "swefwef":
      break;
    default:
      break;
  }
};
